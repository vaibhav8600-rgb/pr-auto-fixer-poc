{
  "name": "pr-auto-fixer-poc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p .",
    "typecheck": "tsc -p . --noEmit",
    "lint": "eslint . --ext .ts",
    "format": "prettier . --write",
    "start": "node dist/index.js",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@octokit/rest": "^21.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.9.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  }
}
